import { Routes } from '@angular/router';
import { TextScreenComponent } from './text-screen/text-screen.component';

export const routes: Routes = [
    {
        path: '',
        component: TextScreenComponent
    }
];